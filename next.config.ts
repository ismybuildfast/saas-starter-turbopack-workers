import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // PPR requires canary version, disabled for now
  // experimental: {
  //   ppr: true,
  //   clientSegmentCache: true
  // }
};

export default nextConfig;
